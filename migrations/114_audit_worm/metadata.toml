[migration]
module = "core"
description = "WORM audit ledger: hash chain columns on audit_log, audit_chain_head, audit_signing_keys, append-only triggers, and vortex_runtime DB role with UPDATE/DELETE revoked on audit_log"
reversible = false
dependencies = ["109_home_screen"]
