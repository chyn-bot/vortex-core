[migration]
module = "core"
description = "Platform sequence service: generic `sequences` table with (code, scope) composite key. All Vortex verticals use this service for atomic, no-gap code generation (leads, invoices, tickets, …) via `vortex_orm::sequence`. Composite PK lets time-based sequences share a logical code across periods."
reversible = true
dependencies = ["116_workflow_engine"]
